мені треба зробити :

в арр -
 доступ до сторінки з контактами. для цього створити файли 
приватних та регістраційних маршрутизацій як це зроблено в прикладі
а саме:
 <RestrictedRoute redirectTo="/tasks" component={<LoginPage />} />
 <PrivateRoute redirectTo="/login" component={<TasksPage />} />

в сторінці контактів
 перевірити чи все працює

на сторінках логіну та реєстрації додати посилання на компоненти
